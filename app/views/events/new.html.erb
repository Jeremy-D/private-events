<h1>Make a new event!</h1>
<%= form_for(@event, url: events_path) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :location %>
	<%= f.text_field :location %>

	<%= f.submit "Ready to Party!" %>
<% end %>